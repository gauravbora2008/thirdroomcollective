---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { seriesList } from "../../data/series";

const base = import.meta.env.BASE_URL;
---

<BaseLayout
  title="Podcasts — Third Room"
  description="Third Room podcast episodes, grouped by topic."
  current="podcast"
>
  <div class="container">
    <header class="page-header">
      <h1>Podcasts</h1>
      <p class="subtitle">Episodes from live discussions. Browse by topic below.</p>
    </header>

    <p class="muted">
      No episodes yet. After each event, add a page here and list it under the
      matching topic.
    </p>

    <h2>By topic</h2>
    <ul class="content-list">
      {
        seriesList.map((topic) => (
          <li>
            <h3>
              <a href={`${base}topics/${topic.id}/`}>{topic.title}</a>
            </h3>
            <p class="sm muted">No episodes yet.</p>
          </li>
        ))
      }
    </ul>
  </div>
</BaseLayout>
