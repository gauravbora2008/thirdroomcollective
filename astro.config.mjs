import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://gauravbora2008.github.io",
  base: "/thirdroomcollective",
  trailingSlash: "always",
});
